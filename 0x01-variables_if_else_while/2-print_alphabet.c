#include <stdio.h>
/* printing the alphabet by Evan R. */

/**
 * main - printing alphabet
 * Return: is 0
 */
int main(void)
{
	int ch;
	/* print all lowercase letters */
		for (ch = 'a'; ch <= 'z'; ch++)

			putchar(ch);

			putchar('\n');
}
