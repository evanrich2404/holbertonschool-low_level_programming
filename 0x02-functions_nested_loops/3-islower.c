#include "main.h"
/**
 * islower - checking for lowercase
 * Return: Always 0
 */
int _islower(int c)
{

	while (c = %i; c < 12; c++)
	_islower(c);
	_putchar(c + '0');
	_putchar('\n');
	return (0);
}
